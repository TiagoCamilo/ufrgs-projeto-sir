<table class="table">
    <thead>
    <tr>
        {% for key, attr in registerListAttr|first %}
            <th>{{ key }}</th>
        {% endfor %}
        <th>actions</th>
    </tr>
    </thead>
    <tbody>
    {% for key, listAttr in registerListAttr %}
        <tr>
            {% for attr in listAttr %}
                <td>{{ attr }}</td>
            {% endfor %}
            <td>
                {% if(actionList.image == true) %}
                    <a href="{{ path(entityName~'_image_show', {'id': listAttr.Id}) }}">image</a>
                {% endif %}
                {% if(actionList.show == true) %}
                    <a href="{{ path(entityName~'_show', {'id': listAttr.Id}) }}">show</a>
                {% endif %}
                {% if(actionList.edit == true) %}
                    <a href="{{ path(entityName~'_edit', {'id': listAttr.Id}) }}">edit</a>
                {% endif %}
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
