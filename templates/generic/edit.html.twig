{% extends 'base.html.twig' %}

{% block title %}Edit {{ entityName|capitalize }}{% endblock %}

{% block body %}
    <h1>Edit {{ entityName|capitalize }}</h1>

    {{ include(template~'/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path(entityName~'_index') }}">back to list</a>

    {{ include(template~'/_delete_form.html.twig') }}
{% endblock %}
