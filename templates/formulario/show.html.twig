{% extends 'base.html.twig' %}

{% block title %}Formulario{% endblock %}

{% block body %}
    <h1>Formulario</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ register.id }}</td>
        </tr>
        <tr>
            <th>Nome</th>
            <td>{{ register.nome }}</td>
        </tr>
        </tbody>
    </table>

    {{ include(template.showActions) }}

{% endblock %}

