{{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.descricao) }}
    {{ form_row(form.data_hora) }}
    {{ form_widget(form) }}

    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}