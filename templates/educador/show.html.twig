{% extends 'base.html.twig' %}

{% block title %}Educador{% endblock %}

{% block body %}
    <h1>Educador</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ educador.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ educador.nome }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('educador_index') }}">back to list</a>

    <a href="{{ path('educador_edit', {'id': educador.id}) }}">edit</a>

    {{ include('educador/_delete_form.html.twig') }}
{% endblock %}
