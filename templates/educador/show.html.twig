{% extends 'base.html.twig' %}

{% block title %}Educador{% endblock %}

{% block body %}
    <h1>Educador</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ register.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ register.nome }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('educador_index') }}">back to list</a>

    <a href="{{ path('educador_edit', {'id': register.id}) }}">edit</a>

    {{ include('educador/_delete_form.html.twig') }}
{% endblock %}
